<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F0F4F8"
    tools:context=".MainActivity">

    <!-- 1. Top Bar -->
    <LinearLayout
        android:id="@+id/topBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@android:color/white"
        android:elevation="4dp"
        android:orientation="vertical"
        android:padding="8dp"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal"
            android:text="ENGLISH TRACKING AI"
            android:textColor="#2563EB"
            android:textSize="18sp"
            android:textStyle="bold"
            android:letterSpacing="0.1" />
    </LinearLayout>

    <!-- 2. Header Actions -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/headerActions"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="12dp"
        android:paddingVertical="8dp"
        app:layout_constraintTop_toBottomOf="@id/topBar">

        <Button
            android:id="@+id/historyButton"
            style="?attr/materialButtonOutlinedStyle"
            android:layout_width="wrap_content"
            android:layout_height="38dp"
            android:text="📅 History"
            android:textSize="9sp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

        <Button
            android:id="@+id/subscriptionButton"
            android:layout_width="wrap_content"
            android:layout_height="38dp"
            android:layout_marginStart="4dp"
            android:backgroundTint="#FFD700"
            android:text="👑 VIP"
            android:textSize="9sp"
            android:textColor="#000000"
            android:textStyle="bold"
            app:layout_constraintStart_toEndOf="@id/historyButton"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

        <Button
            android:id="@+id/levelButton"
            android:layout_width="wrap_content"
            android:layout_height="38dp"
            android:backgroundTint="#2563EB"
            android:text="Level: Beginner"
            android:textSize="9sp"
            android:textColor="@android:color/white"
            android:layout_marginEnd="8dp"
            app:layout_constraintEnd_toStartOf="@id/profileContainer"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

        <FrameLayout
            android:id="@+id/profileContainer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent">

            <Button
                android:id="@+id/loginNavigateButton"
                android:layout_width="wrap_content"
                android:layout_height="38dp"
                android:backgroundTint="#2196F3"
                android:text="Login"
                android:textSize="9sp"
                android:textColor="@android:color/white" />

            <ImageView
                android:id="@+id/profileButton"
                android:layout_width="38dp"
                android:layout_height="38dp"
                android:padding="4dp"
                android:src="@android:drawable/ic_menu_myplaces"
                android:visibility="gone"
                app:tint="#2563EB" />
        </FrameLayout>
    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- 3. AI Card with Character (HIDDEN) -->
    <androidx.cardview.widget.CardView
        android:id="@+id/avatarCard"
        android:layout_width="match_parent"
        android:layout_height="220dp"
        android:layout_marginHorizontal="16dp"
        android:layout_marginTop="8dp"
        android:visibility="gone"
        app:cardCornerRadius="20dp"
        app:cardElevation="6dp"
        app:layout_constraintTop_toBottomOf="@id/headerActions">

        <VideoView
            android:id="@+id/avatarVideoView"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <View
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:layout_gravity="bottom"
            android:background="@drawable/gradient_shadow" />

        <TextView
            android:id="@+id/voiceStatusTextView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|center_horizontal"
            android:layout_marginBottom="12dp"
            android:background="#99000000"
            android:paddingHorizontal="12dp"
            android:paddingVertical="4dp"
            android:text="HOLD MIC TO TALK"
            android:textColor="@android:color/white"
            android:textSize="12sp"
            android:textStyle="bold" />
    </androidx.cardview.widget.CardView>

    <!-- Status labels moved under card -->
    <LinearLayout
        android:id="@+id/statsBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="16dp"
        android:paddingTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/avatarCard">
        <TextView android:id="@+id/userNameTextView" android:layout_width="0dp" android:layout_weight="1" android:layout_height="wrap_content" android:text="Welcome Guest 👋" android:textSize="12sp" android:textStyle="bold" />
        <TextView android:id="@+id/userStatusBadge" android:layout_width="wrap_content" android:layout_height="wrap_content" android:text="AI: 20 Left" android:textSize="11sp" android:textColor="#1976D2" android:background="#E3F2FD" android:paddingHorizontal="8dp" android:paddingVertical="2dp" android:visibility="gone" />
    </LinearLayout>

    <!-- 4. Chat Area (HIDDEN) -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/chatRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginHorizontal="16dp"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="16dp"
        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@id/statsBar"
        app:layout_constraintBottom_toTopOf="@id/bottomSection" />

    <!-- 5. Interactive Bottom Section -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/bottomSection"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp"
        android:background="@android:color/white"
        app:layout_constraintBottom_toBottomOf="parent">

        <!-- Action Grid -->
        <GridLayout
            android:id="@+id/actionGrid"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:columnCount="2"
            app:layout_constraintTop_toTopOf="parent">

            <Button android:id="@+id/practiceButton" android:layout_width="0dp" android:layout_columnWeight="1" android:layout_margin="4dp" android:text="📖 Practice" android:backgroundTint="#FF9800" android:textSize="11sp" android:visibility="gone" />
            <Button android:id="@+id/vocabButton" android:layout_width="0dp" android:layout_columnWeight="1" android:layout_margin="4dp" android:text="🔤 Words" android:backgroundTint="#9C27B0" android:textSize="11sp" />
            <Button android:id="@+id/testButton" android:layout_width="0dp" android:layout_columnWeight="1" android:layout_margin="4dp" android:text="📝 Test" android:backgroundTint="#E91E63" android:textSize="11sp" />
            <Button android:id="@+id/connectBuddyButton" android:layout_width="0dp" android:layout_columnWeight="1" android:layout_margin="4dp" android:text="📞 Buddy Call" android:backgroundTint="#4CAF50" android:textSize="11sp" />

        </GridLayout>

        <!-- The Big Mic Button (HIDDEN) -->
        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/micButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="16dp"
            android:src="@android:drawable/ic_btn_speak_now"
            android:visibility="gone"
            app:backgroundTint="#2563EB"
            app:tint="@android:color/white"
            app:fabSize="normal"
            app:layout_constraintTop_toBottomOf="@id/actionGrid"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

        <!-- Gender Filter below everything -->
        <RadioGroup
            android:id="@+id/buddyGenderGroup"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            app:layout_constraintTop_toBottomOf="@id/actionGrid"
            app:layout_constraintEnd_toEndOf="parent"
            android:visibility="gone">
            <RadioButton android:id="@+id/rbAny" android:layout_width="wrap_content" android:layout_height="wrap_content" android:text="Any" android:checked="true" android:textSize="10sp" />
            <RadioButton android:id="@+id/rbMaleBuddy" android:layout_width="wrap_content" android:layout_height="wrap_content" android:text="M" android:textSize="10sp" />
            <RadioButton android:id="@+id/rbFemaleBuddy" android:layout_width="wrap_content" android:layout_height="wrap_content" android:text="F" android:textSize="10sp" />
        </RadioGroup>

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.constraintlayout.widget.ConstraintLayout>